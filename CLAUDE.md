# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 할 규칙을 정의합니다.

## 저장소 성격

서강대학교 AI·SW 대학원 개인 학습 아카이브입니다. **코드 프로젝트가 아니라 문서
저장소**이므로 빌드·테스트·린트가 없습니다. 강의 원본 자료(PPT/PDF/노트북/데이터)를
그대로 보관하고, 그 옆에 직접 정리한 `README.md` 요약을 두는 것이 전부입니다.

핵심 원칙: **원본은 손대지 않고, 요약은 실제 자료를 읽고 사실 확인해서 쓴다.**
강의계획서를 그대로 옮겨 적는 것이 아니라, 실제로 다룬 내용을 정리합니다.

## 디렉터리 구조

```
<학년>-<학기>.<Course_Name_Snake_Case>/   # 과목 (예: 1-2.AI_Data_System_Infra)
├── README.md                             # 과목 개요 + 목록 (필수)
├── syllabus.pdf                          # 있으면 보관, 없어도 됨
├── NN-topic-kebab-case/                  # 1-1 방식: 주차별 폴더 + 원본 자료 + README.md
└── NN-topic-kebab-case.md                # 1-2 방식: 주차별 단일 마크다운 노트

SEMINAR/<Topic_Snake_Case>/               # 외부 세미나·강연 (정규 수업과 분리)
README.md                                 # 루트: 영어, 과목 표로 전체 인덱스
```

### 네이밍

| 대상 | 규칙 | 예시 |
|------|------|------|
| 과목 디렉터리 | `<학년>-<학기>.<Snake_Case>` | `1-1.Linux_OS_and_Applications` |
| 세미나 디렉터리 | `SEMINAR/<Snake_Case>` | `SEMINAR/Reinforcement_Learning` |
| 주차 폴더/노트 | `NN-kebab-case` (2자리, 실제 주제명) | `09-concurrency-and-locks` |
| 자료 파일 | `kebab-case.<ext>` (내용을 설명하는 이름) | `cpu-scheduling-slides.pdf` |

- 주차 번호는 **실제 강의 주차**를 따릅니다. 한 주제가 여러 주에 걸치면 `02-03-numpy-arrays`
  처럼 범위로 씁니다. 자료가 없는 주차도 `07-no-materials/`로 자리를 남깁니다.
- `midterm-exam/`, `final-project/`처럼 시험·프로젝트 제출물은 번호 없이 이름으로 둡니다.
- "Week N" 같은 무의미한 이름은 쓰지 않습니다. 폴더명만 봐도 주제를 알 수 있어야 합니다.

### 1-1 방식과 1-2 방식

- **1-1 (완료된 과목)**: 주차마다 폴더를 만들고 원본 슬라이드·데이터·제출물과 `README.md`를
  함께 둡니다.
- **1-2 (진행 중인 과목)**: 원본 슬라이드 없이 수업 중 필기를 `NN-topic.md` 단일 파일로
  정리합니다.

기존 과목의 방식을 바꾸지 말고, 그 과목이 이미 쓰고 있는 방식을 따르세요.

## README 규칙

### 언어

- **루트 `README.md`만 영어**입니다.
- **과목·주차 README는 모두 한국어**입니다. 강의가 한국어로 진행되므로 용어도 한국어를
  기본으로 하되, 영문 기술 용어는 그대로 씁니다 (예: "페이지 교체 정책", "KV Cache").

### 과목 README 구조 (고정)

```markdown
# <English Course Name>

<br/>

서강대학교 AI·SW 대학원 **<한글 과목명>(<학수번호>)** 강의 내용을 정리한
저장소입니다. 담당 교수는 <이름>(`<email>`)이며, <한 문단으로 과목이 다루는 흐름>.

<br/>

> *이 저장소의 자료는 학습 및 개인 복습 목적으로 정리한 것이며, 실제 강의
> 내용과 표현상 차이가 있을 수 있습니다.*

<br/>

## 강의 개요
| 항목 | 내용 |          <- 학수번호 / 학기 / 학점 / 시간 / 난이도 / 수업형태 / 평가
**교과목표** / **참고문헌** / **운영 특징**   <- 강의계획서에 있는 항목만

### 주차별 계획 (강의계획서 기준)
| 주차 | 주제 | ... |    <- 16주 전체. 시험 주차는 《중간고사》 표기

## 목록
| 순서 | 주제 | 자료 |   <- 실제 보관 중인 자료 (1-1) 또는 정리 진행 상황 (1-2)

## 구성
<디렉터리 트리 코드 블록>   <- 1-1 방식일 때만
```

- 섹션 사이는 `<br/>`로 띄웁니다 (기존 문서 스타일).
- `## 강의 개요`는 강의계획서 기준, `## 목록`은 실제 자료 기준입니다. **둘이 어긋나면
  어긋난다고 명시**하세요 (예: 실제 진행 순서가 강의계획서와 다른 경우).
- 1-2 과목의 `## 목록`은 완료된 주차만 링크를 걸고 나머지는 `예정`으로 둡니다.

## 새 자료를 추가할 때

1. 해당 과목 디렉터리에 규칙에 맞는 이름으로 파일/폴더를 추가합니다.
2. **과목 `README.md`의 목록을 갱신**합니다 (1-1이면 `## 구성` 트리도).
3. 새 과목이면 **루트 `README.md`의 트랙별 표에 행을 추가**합니다.
   - 트랙: `Software Engineering / Systems` 또는 `Data Analysis / AI`
   - 최신 학기가 표 위쪽에 오도록 정렬합니다.
   - 학기 표기는 영어로 `2026 Spring` / `2026 Fall` (1학기 = Spring, 2학기 = Fall).
4. 디렉터리 이름을 바꿨다면 **모든 상대 링크와 `## 구성` 트리를 함께 고칩니다.**
   커밋 전에 링크가 실제 경로로 해석되는지 확인하세요.

## 작업 시 주의

- **원본 자료를 편집하지 않습니다.** PPT/PDF/노트북/데이터는 받은 그대로 둡니다.
  노트북 설명에 오류가 있으면 원본을 고치지 말고 README에 정정 사항으로 적습니다.
- **모르는 값을 지어내지 않습니다.** 학수번호, 교수 이메일, 강의 시간 등은 강의계획서나
  사용자에게 확인한 것만 씁니다. 확인이 안 되면 `TBD`로 두고 사용자에게 물어봅니다.
- `temp/`는 참고용 원본 보관 위치로 `.gitignore`되어 있습니다. 여기에 있는 파일을
  커밋하지 마세요.
- 커밋 메시지는 Conventional Commits를 따르며, 이 저장소는 사실상 전부 `docs:`입니다.
  본문은 영어로 씁니다.
